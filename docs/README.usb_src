This is the usb_src-write-a-block package meant as a guide to building
out-of-tree packages. To use the usb_src blocks, the Python namespaces
is in 'usb_src', which is imported as:

    import usb_src

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(usb_src)
